# Direct dependencies, pinned. Transitive deps resolve automatically.
# Regenerate with: pip freeze | grep -iE '^(fastapi|uvicorn|...)=='

# Web
fastapi==0.141.1
uvicorn==0.53.0
python-multipart==0.0.32

# Data
duckdb==1.5.5
pandas==3.0.6

# SQL validation (Stage 10)
sqlglot==30.18.0

# Gemini (Stages 5 and 10)
google-genai==2.24.0

# Tests
pytest==9.1.1
